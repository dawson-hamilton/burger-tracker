<li>
    {{name}}

    <button class="change-devoured btn btn-primary btn-sm" data-id="{{id}}" data-newBurger="{{devoured}}">
        {{#if devoured}}YUMMY!{{else}}EAT-DA-BURGER{{/if}}
    </button>
</li>
<br>